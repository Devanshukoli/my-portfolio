// markdown utils removed: blog content is now served from server endpoints (/api/posts)
// If you need programmatic access to markdown files in the future, use server-side
// helpers or import from the `data/blog` directory directly in node scripts.

export const deprecated = true;
